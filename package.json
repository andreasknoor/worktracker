{
  "name": "worktracker",
  "version": "1.6",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@worktracker/core",
    "dev": "npm run build && node --env-file=.env.local --import tsx src/server/devServer.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc -b packages/core --noEmit",
    "db:apply-schema": "node scripts/apply-schema.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@worktracker/core": "*",
    "hono": "^4.6.14",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
